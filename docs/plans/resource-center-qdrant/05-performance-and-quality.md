# P5 性能与质量

> 状态：`DONE`（本地原创数据基线；残留清理已获准作为收尾例外）
> 里程碑：M5 性能质量达标
> 前置依赖：[P4 生产就绪](04-production-readiness.md) `DONE`
> 后续阶段：[P6 多租户、高可用与智能增强](06-multitenancy-ha-and-intelligence.md)
> 模型配置：实验可提出候选模型和参数，但只有管理员能在管理端测试并激活模型；代码、环境变量和基准脚本不得直接切换 active 版本。
> 执行约束：P2-A 暂停门已于 2026-09-06 明确解除；当前阶段仍须满足上述前置里程碑后才能启动。
> 本轮范围：项目负责人已授权“没有真实语料，你直接自己编写测试即可”；使用原创合成材料继续功能与集成验证，不以真实教学语料缺失为测试阻断。
> 当前执行：P5 11/11、M5 本地性能/质量门禁通过。项目负责人确认不影响功能与验收的测试残留可记录后完成；清理例外已登记，P5 收尾完成，P6 未启动。证据见[本轮验收](TEST-P5-2026-09-08.md)。

## 1. 阶段目标

使用固定、代表性的语料和查询集优化索引、collection、批处理、缓存、切块、融合与重排参数，使性能、质量和成本达到 P0 批准的 SLO。没有目标规模实测证据时，不凭开发小数据集修改生产参数。

## 2. 工作清单

以下 11 项按批准的本地原创数据基线完成；停止的隔离测试环境残留仅占用本地磁盘，不影响功能和验收，已由项目负责人接受为收尾例外。完整数值、失败记录与范围见[本轮验收](TEST-P5-2026-09-08.md)。

- [x] **P5-01 数据固化**：自行编写 60 份四格式短讲义、120 个独立问句并冻结摘要，正常/降级共 240 正例；结合既有长文本、格式、权限和更新边界。容量副本不计作新增语义数据。
- [x] **P5-02 索引对比**：保留既有量化/磁盘/sparse 对比及失败记录；同十万点两次重建校准 ef，2048 时 ANN Recall@10 为 0.945/0.943，多副本为 0.985；不推广损失明显的量化。
- [x] **P5-03 拓扑决策**：完成单节点与 3 shard/3 replica/写一致性 2 的同数据对照。保留每知识库/generation 独立 collection；本地三 peer 不外推跨可用区。
- [x] **P5-04 入库参数决策**：结合 101 个 Mock 子场景，真实四格式入库与重建通过，60 文档约 29–31 秒；记录输入字节、调用、队列，保留 batch/worker 并发及 wait 确认，不用短文档吞吐提高生产并发。
- [x] **P5-05 缓存决策**：预取真实 query 向量测量本地开销，撤权后的 20 次查询与旧引用拒绝通过。当前继续不启用应用查询/授权缓存；恢复补充 ANALYZE，生成列预计算不缓存权限。
- [x] **P5-06 切块决策**：结合既有 152 个边界与 6 组证据窗口对照，真实四格式默认切块完成质量验收；保留当前切块/重叠参数，输入字节不冒充供应商精确 token。
- [x] **P5-07 召回融合**：生成列减少重复分词，当前 manifest 筛选后去重，融合授权后再次按正文去重；保留既有 RRF 权重/TopK，修复冷请求过早取消，最终质量与容量通过。
- [x] **P5-08 重排决策**：管理员当前未配置启用 reranker，实查 disabled、provider 调用 0；结合既有 9 种 Mock 模式保持跳过/降级策略，未声称真实重排增益或擅自激活模型。
- [x] **P5-09 质量回归**：正常/降级分组、权限、引用与五次真实 Tutor 缺失资料验证通过；版本化评估器及并发评估器可阻断排名、引用、授权、时长或预算回归。
- [x] **P5-10 负载稳定性**：十万块五并发真实模型、正式 HTTPS、无节流及十分钟持续读取通过；另完成十分钟数值读写混合、恢复和重建，队列清零，无持续连接/内存积累证据。
- [x] **P5-11 决策与回滚**：默认保持非量化、无应用缓存、现有切块/worker/RRF；新增可配置 ef，局部验证使用 2048、部署默认仍 0。真实 ready→promote→rollback 通过；发布顺序、监控与回滚写入运维/迁移手册，外部上线另行安排。

## 3. 实验纪律

1. 每次实验只改变一组相关变量，保留基线、样本版本、随机种子、硬件、依赖版本和配置。
2. 质量、性能、成本和运维复杂度共同评估，不能只追求单一最高 Recall。
3. 所有结果按权限过滤后的最终输出计算，不用未授权候选抬高离线指标。
4. 缓存测试必须覆盖租户/用户/ACL/generation 变化和失效，不能只测命中性能。
5. 索引参数调整通过新 generation 构建和门禁切换，不原地破坏当前可服务索引。
6. 小型开发数据只用于可执行性验证，不作为生产选型结论。
7. 模型实验使用独立候选版本；通过质量、成本和安全门后仍须由管理员执行激活，实验工具不得直接改写 active 配置。

## 4. 最小结果表

| 类别 | 必须记录 |
|---|---|
| 数据 | 文档、版本、chunk、向量数量，长度/类型/权限分布 |
| 环境 | Qdrant/client/Go 版本、节点规格、shard、replication factor、网络和存储 |
| 索引 | 类型、HNSW/量化参数、payload index、build/optimizer 时间、内存、磁盘 |
| 查询 | 并发、QPS、P50/P95/P99、timeout、error、degrade |
| 质量 | Recall@K、MRR/nDCG、引用正确率、无答案和权限通过率 |
| 入库 | 文档/分钟、chunk/秒、embedding 成本、backlog age、失败率 |
| 成本 | embedding/rerank 调用、计算、内存、磁盘和网络成本 |

## 5. 验证计划

- 使用版本化评测数据和可重复命令生成结果；数据含敏感内容时只保存受控位置和匿名摘要。
- 临时 benchmark/测试源码在记录结果后删除，不进入提交。
- 执行在线检索与后台重建混合负载，验证对 API 和 worker 的相互影响。
- 在缓存启用/禁用、rerank 启用/禁用、Qdrant 降级三种模式下重复质量与性能检查，并覆盖 payload filter 选择性和 snapshot 恢复后的冷启动。
- 进行至少一次达到目标时长的 soak，检查内存、连接、segment、任务和错误累积。
- 参数发布使用 generation 蓝绿流程并验证回滚。

## 6. 阶段退出条件

- 固定数据与查询集下达到 D-004、D-005 的全部门槛。
- 选定索引、量化、collection、shard/replication、批处理和缓存参数均有可重复证据。
- 混合负载和 soak 无持续资源泄漏、backlog 失控或权限缓存串扰。
- 质量报告能阻止明显相关性、引用、无答案和权限回归。
- 每项优化都有适用范围、监控指标和回滚阈值。

## 7. 完成记录

| 字段 | 内容 |
|---|---|
| 状态 | `DONE`（11/11，M5 本地通过；清理残留按批准例外保留） |
| 负责人 | Codex |
| 开始日期 | 2026-09-07 |
| 完成日期 | 2026-09-08 |
| 验证命令 | 原创真实质量/入库/恢复/发布/容量入口；四包最终 race + coverage、全仓 vet/build、前端 lint/build；Python unittest/trace；完整口径见本轮验收 |
| 验证结果 | 最终整次容量验收通过：五并发真实模型最长 2355.635 ms；正式 HTTPS P95 947.896 ms，突发 914.276 ms，十分钟 824.816 ms；质量、引用、权限、发布回滚与恢复通过 |
| 覆盖率 | 新增/修改重点函数 81.3%–100%；词法 Mock 94.7%、真实 SQL 合并 100%；Python 并发评估器可执行行 99%；大包/全仓不宣称达到 80% |
| 交付物 | `0024` 词法生成列、SQL/授权后去重、召回预算、HNSW ef 配置、并发评估、恢复统计与导入导出兼容；本轮验收和相关手册 |
| 回滚验证 | 真实 ready→promote→rollback 和十万块 PG/Qdrant 恢复通过；应用回滚保留生成列与旧索引，未修改管理员 active 模型 |
| 遗留风险 | 测试残留在忽略目录占用磁盘，服务已停止且残留不纳入交付，已获准不阻断完成；合成/本地基线不替代真实学期数据、外部上线或跨可用区验收 |

以下为此前记录，不代表当前执行状态。

2026-09-08 此前收尾：生产修复为 `88695f9` 的中文词法索引；[中文验收](TEST-HAN-2026-09-08.md)、[真实索引对比](TEST-INDEX-2026-09-08.md)、[文本测试](TEST-TEXT-TUNING-2026-09-08.md)、[入库测试](TEST-INGESTION-TUNING-2026-09-08.md)分别保存证据。文本 8 个顶层/42 子测试和入库 7 个顶层/101 子场景通过 race；十万条索引实验因预设质量门禁未达标返回失败，不能写成全部测试通过。临时测试源码和独立 Qdrant 容器/collection 已清理；旧临时 PG 实例目录与部分日志/覆盖文件因自动审批拒绝清理仍保留于忽略目录，PG 已停止。P5-02 完成、P5 1/11，其他项保留未完成；不继续 P6。

2026-09-08 后续原创合成验证：[详细报告](TEST-SYNTHETIC-2026-09-08.md)。真实 PG 22 个迁移、正式 HTTPS/检索/引用、125 个冻结场景、288 次引用、授权竞态、152 个切块场景、26 个错误边界及 250 次并发读取通过 race 检查；重点函数覆盖 82.4%–92.6%。精确词条通过与中文改写 FTS 0/24 分开记录。临时测试与 fixture 按规则清理；P5 仍为 0/11 完整任务，不将局部验证计作整个调优任务完成。
