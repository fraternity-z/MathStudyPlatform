# P5 性能与质量

> 状态：`IN_PROGRESS`
> 里程碑：M5 性能质量达标
> 前置依赖：[P4 生产就绪](04-production-readiness.md) `DONE`
> 后续阶段：[P6 多租户、高可用与智能增强](06-multitenancy-ha-and-intelligence.md)
> 模型配置：实验可提出候选模型和参数，但只有管理员能在管理端测试并激活模型；代码、环境变量和基准脚本不得直接切换 active 版本。
> 执行约束：P2-A 暂停门已于 2026-09-06 明确解除；当前阶段仍须满足上述前置里程碑后才能启动。
> 本轮范围：项目负责人已授权“没有真实语料，你直接自己编写测试即可”；使用原创合成材料继续功能与集成验证，不以真实教学语料缺失为测试阻断。
> 当前执行：2026-09-08 按项目负责人“完成这一阶段中断目标并更新文档，不要继续往下推了”收尾已启动的验证；停止新增开发和实验，不进入 P6。P5 为 1/11，M5 未通过，保留 `IN_PROGRESS` 表示尚未完成。

## 1. 阶段目标

使用固定、代表性的语料和查询集优化索引、collection、批处理、缓存、切块、融合与重排参数，使性能、质量和成本达到 P0 批准的 SLO。没有目标规模实测证据时，不凭开发小数据集修改生产参数。

## 2. 工作清单

- [ ] **P5-01 代表性数据固化**（`IN_PROGRESS`）：已交付版本化样本契约、checksum 校验和 24 篇原创数学材料的本轮冻结证据；后续扩充合成材料的格式、长度、权限与更新分布，不要求先提供真实教学语料。
- [x] **P5-02 索引对比**：固定 Qdrant 1.14.1，完成六组一万条和八组十万条 HNSW、scalar/product/binary、on-disk、sparse 对比，记录上传/索引、内存/磁盘、Recall 与时延。十万条 HNSW 数值近邻 Recall@10 仅 0.541，实验门禁失败，不发布新参数；对比完成不等于 M5 达标。见[索引实验与收尾](TEST-INDEX-2026-09-08.md)。
- [ ] **P5-03 Collection/shard/replica 调优**（`IN_PROGRESS`）：已完成十万条 1/3 shard、payload index 有/无及 segment 观察；replica、cache、共享粒度与完整隔离仍未验收，保留现有配置。
- [ ] **P5-04 入库批处理调优**（`IN_PROGRESS`）：[101 个工程子场景](TEST-INGESTION-TUNING-2026-09-08.md)及 race 通过，验证 active batch 8/32/64、110000 字节预算、并发 1/2/8、有限重试、背压与状态围栏；真实吞吐/费用、wait/ordering、跨副本及混合重建负载未验收。
- [ ] **P5-05 缓存与预加载**（`IN_PROGRESS`）：已核对无应用检索/embedding 缓存，重复请求重新召回与最终鉴权通过；未实现或测量缓存收益与预加载，完整失效和成本收益仍待验证。
- [ ] **P5-06 切块实验**（`IN_PROGRESS`）：原 152 个边界场景之外，新增 6 篇/72 条数学事实的 6 组证据窗口实验；128/32 反而降低单块事实完整率，512/128 增加 27.4% 字节，未据此修改生产参数。真实质量和 token 成本未完成。
- [ ] **P5-07 召回融合调优**（`IN_PROGRESS`）：`0023` 修复中文词法召回，新编 30 条改写由旧谓词 0/30 提高到 Recall@5 1.0；RRF 去重/权限及手工候选参数敏感性通过，真实融合 TopK/权重/阈值尚未验收。
- [ ] **P5-08 Rerank 调优**（`IN_PROGRESS`）：[文本验证](TEST-TEXT-TUNING-2026-09-08.md)覆盖 9 种 rerank 模式、候选/字节上限、超时与撤权；真实模型、排名增益和成本未评估，继续保持现有跳过/降级契约。
- [ ] **P5-09 质量回归门禁**（`IN_PROGRESS`）：保留原 125 场景/288 引用证据，新增中文词法、错误路径和权限检查通过；十万条数值 ANN 质量门禁明确失败。本轮没有真实模型或 Tutor 调用，完整 M5 未通过。
- [ ] **P5-10 负载与稳定性测试**（`IN_PROGRESS`）：十万 PostgreSQL 块、5 并发/250 请求 P95 206.90 ms 且无错误；真实 Qdrant 十万条八组各 100 次查询完成，仍缺真实模型全链路、混合读写/重建/soak、backlog 与费用验收。
- [ ] **P5-11 参数决策与发布**（`IN_PROGRESS`）：本轮决定保留现有索引、量化、切块、融合及 worker 参数，未批准候选发布；完整参数发布/回滚和观察窗口尚未执行。按用户指令停止在当前收尾点。

## 3. 实验纪律

1. 每次实验只改变一组相关变量，保留基线、样本版本、随机种子、硬件、依赖版本和配置。
2. 质量、性能、成本和运维复杂度共同评估，不能只追求单一最高 Recall。
3. 所有结果按权限过滤后的最终输出计算，不用未授权候选抬高离线指标。
4. 缓存测试必须覆盖租户/用户/ACL/generation 变化和失效，不能只测命中性能。
5. 索引参数调整通过新 generation 构建和门禁切换，不原地破坏当前可服务索引。
6. 小型开发数据只用于可执行性验证，不作为生产选型结论。
7. 模型实验使用独立候选版本；通过质量、成本和安全门后仍须由管理员执行激活，实验工具不得直接改写 active 配置。

## 4. 最小结果表

| 类别 | 必须记录 |
|---|---|
| 数据 | 文档、版本、chunk、向量数量，长度/类型/权限分布 |
| 环境 | Qdrant/client/Go 版本、节点规格、shard、replication factor、网络和存储 |
| 索引 | 类型、HNSW/量化参数、payload index、build/optimizer 时间、内存、磁盘 |
| 查询 | 并发、QPS、P50/P95/P99、timeout、error、degrade |
| 质量 | Recall@K、MRR/nDCG、引用正确率、无答案和权限通过率 |
| 入库 | 文档/分钟、chunk/秒、embedding 成本、backlog age、失败率 |
| 成本 | embedding/rerank 调用、计算、内存、磁盘和网络成本 |

## 5. 验证计划

- 使用版本化评测数据和可重复命令生成结果；数据含敏感内容时只保存受控位置和匿名摘要。
- 临时 benchmark/测试源码在记录结果后删除，不进入提交。
- 执行在线检索与后台重建混合负载，验证对 API 和 worker 的相互影响。
- 在缓存启用/禁用、rerank 启用/禁用、Qdrant 降级三种模式下重复质量与性能检查，并覆盖 payload filter 选择性和 snapshot 恢复后的冷启动。
- 进行至少一次达到目标时长的 soak，检查内存、连接、segment、任务和错误累积。
- 参数发布使用 generation 蓝绿流程并验证回滚。

## 6. 阶段退出条件

- 固定数据与查询集下达到 D-004、D-005 的全部门槛。
- 选定索引、量化、collection、shard/replication、批处理和缓存参数均有可重复证据。
- 混合负载和 soak 无持续资源泄漏、backlog 失控或权限缓存串扰。
- 质量报告能阻止明显相关性、引用、无答案和权限回归。
- 每项优化都有适用范围、监控指标和回滚阈值。

## 7. 完成记录

| 字段 | 内容 |
|---|---|
| 状态 | `IN_PROGRESS`（本轮收尾后停止推进，P5 1/11，M5 未通过） |
| 负责人 | Codex |
| 开始日期 | 2026-09-07 |
| 完成日期 |  |
| 验证命令 | Python 临时 unittest + trace；Go 中文/文本/入库 race + coverage；真实 Qdrant 索引实验；后端 test/vet/build、前端 lint/build；各报告保存对应命令 |
| 验证结果 | 中文、文本及入库局部契约通过；一万条索引实验通过，十万条命令因 ANN 质量门禁未达标失败；未调用真实模型，未发布候选参数 |
| 覆盖率 | Python 评估器可执行行 99%；SearchLexical 100%，文本重点算法 91.9%–100%，入库重点函数 80%–100%；各大包覆盖低于 80%，不合并成全仓覆盖结论 |
| 交付物 | `0023` 中文词法索引及代码；质量评估器/手册；中文、索引、文本、入库四份新报告和本页/总进度更新 |
| 回滚验证 | 本轮未执行新的 generation 参数发布/回滚；`0023` 为增量函数/索引，应用回滚约束见迁移手册；不修改管理员 active 模型 |
| 遗留风险 | 更广代表性数据、真实模型完整质量、十万条 ANN 校准、混合负载/soak、完整参数发布仍未验收；按用户指令停止，未将 Mock 结论用于放行 M5 |

2026-09-08 本轮收尾：生产修复为 `88695f9` 的中文词法索引；[中文验收](TEST-HAN-2026-09-08.md)、[真实索引对比](TEST-INDEX-2026-09-08.md)、[文本测试](TEST-TEXT-TUNING-2026-09-08.md)、[入库测试](TEST-INGESTION-TUNING-2026-09-08.md)分别保存证据。文本 8 个顶层/42 子测试和入库 7 个顶层/101 子场景通过 race；十万条索引实验因预设质量门禁未达标返回失败，不能写成全部测试通过。临时测试源码和独立 Qdrant 容器/collection 已清理；旧临时 PG 实例目录与部分日志/覆盖文件因自动审批拒绝清理仍保留于忽略目录，PG 已停止。P5-02 完成、P5 1/11，其他项保留未完成；不继续 P6。

2026-09-08 后续原创合成验证：[详细报告](TEST-SYNTHETIC-2026-09-08.md)。真实 PG 22 个迁移、正式 HTTPS/检索/引用、125 个冻结场景、288 次引用、授权竞态、152 个切块场景、26 个错误边界及 250 次并发读取通过 race 检查；重点函数覆盖 82.4%–92.6%。精确词条通过与中文改写 FTS 0/24 分开记录。临时测试与 fixture 按规则清理；P5 仍为 0/11 完整任务，不将局部验证计作整个调优任务完成。
